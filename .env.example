# PlumblineUI v2 is a static Vite/React site.
# No runtime environment variables are required for local development, build, or static hosting.
# Add VITE_* variables here only when a client-side feature actually consumes them.
